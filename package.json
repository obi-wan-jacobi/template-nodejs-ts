{
  "name": "button",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "lint-fix": "./node_modules/.bin/tslint --project tsconfig.json --fix src/**/*.ts",
    "lint": "./node_modules/.bin/tslint --project tsconfig.json",
    "build": "npm run lint-fix && npm run lint && tsc && npm run prune",
    "test": "jest --env=node --colors test",
    "prune": "rimraf ./build/*.js"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/jest": "^26.0.15",
    "@types/node": "^14.14.6",
    "jest": "^26.6.3",
    "rimraf": "^3.0.2",
    "ts-jest": "^26.4.3",
    "tslint": "^5.20.1",
    "tslint-consistent-codestyle": "^1.16.0",
    "typescript": "^3.9.7"
  },
  "author": "",
  "license": ""
}
